{%- import "attr/common.j2" as Common -%}

{# EDITABLE #}
{%- macro __editable_node(type, tsts, value, prefix) -%}
{%- if type == "XPath" -%}
<a:{{prefix}}a name="EDITABLE" type="XPath">
    {%- for tst in tsts -%}
    <a:tst  expr="{{-tst.expr-}}" 
            {{-Common.set_attr("true", tst.true_text)-}}
            {{-Common.set_attr("false", tst.false_text)-}} />
    {%- endfor -%}
</a:{{prefix}}a>
{%- else -%}
    {%- if value is not none -%}
        {%- if value is true -%}
<a:{{prefix}}a name="EDITABLE" value="true"/>
        {%- else -%}
<a:{{prefix}}a name="EDITABLE" value="false"/>
        {%- endif -%}
    {%- endif -%}
{%- endif -%}
{%- endmacro -%}

{%- macro editable_node(config, prefix) -%}
{{__editable_node(config.type, config.tsts, config.value, prefix)}}
{%- endmacro -%}